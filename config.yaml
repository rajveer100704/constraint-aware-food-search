catalog:
  default_path: "data/processed_catalog.json"

evaluation:
  default_set_path: "data/evaluation_set_v2.json"

click_logs:
  default_path: "data/query_click_logs_v1.csv"

models:
  dense_model_name: "sentence-transformers/all-MiniLM-L6-v2"
  ltr_model_path: "models/ltr_model.pkl"

ranking_weights:
  bm25_weight: 0.30
  dense_weight: 0.25
  rating_weight: 0.15
  price_weight: 0.10
  popularity_weight: 0.08
  cuisine_weight: 0.07
  dish_match_weight: 0.05

rrf:
  k_constant: 60
